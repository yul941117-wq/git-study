1. git branch
    -현재 작업중인 브랜치와 목록을 출력

2. git branch -v
    -커밋 내용을 함께 출력

3. git branch 브랜치명
    - 로컬에 새로운 브랜치 생성

4. git branch -d 브랜치명
    -로컬 브랜치 삭제

5. git branch -m 변경할 브랜치명 변경될 브랜치명
    -로컬 브랜치 이름 변경

6.git branch -a
    -로컬과 원격 저장소 브랜치 목록 출력
-------------------------------------------------

1. git init
    - 해당 경로에 설정 초기화
    - 해당 경로에 .git 폴더가 생김

2. git clone 저장소
    -원격 저장소를 로컬로 가져오기

3. git add 파일명
    -파일을 staging area에 추가

4. git commit -m "메세지"
    -staging area에 있는 파일을 local repositori에 추가
    -원격 저장소에 올리기 전, 변경 사항이 모두 저장된 파일을 의미

5. git push 리모트이름 원격저장소브랜치명
    -원격 저장소로 로컬 코드 올리기

6. git pull 리모트이름 원격저장소브랜치명
    -원격 저장소의 코드를 가져오기

7. git status
    -깃이 추적중인 상태의 파일 확인

8. git log
    -commit 기록 확인 (나가기 : q)
    
